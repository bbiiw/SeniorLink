# ใช้ Node.js เป็น Base Image
FROM node:20

# ตั้งค่า Working Directory
WORKDIR /app

# คัดลอก package.json และ package-lock.json
COPY package*.json ./

# ลบ node_modules และ package-lock.json ถ้ามีอยู่
RUN rm -rf node_modules package-lock.json

# ติดตั้ง Dependencies
RUN npm install

# คัดลอกโค้ดทั้งหมด
COPY . .

# สร้างไฟล์ production โดยใช้คำสั่ง build
RUN npm run build

# เปิดพอร์ต 5173 สำหรับการรันเซิร์ฟเวอร์
EXPOSE 5173

# Step 2: Serve the app with Nginx (ถ้าใช้ Nginx ใน production)
# CMD ["npm", "run", "serve"]

# หรือถ้าไม่ได้ใช้ Nginx ก็สามารถใช้เซิร์ฟเวอร์จาก Vite โดยตรง
CMD ["npm", "run", "dev"]
