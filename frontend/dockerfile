# Dockerfile for React frontend in production

# Step 1: Build React app with Node.js
FROM node:20 AS build

# ตั้งค่า Working Directory
WORKDIR /app

# คัดลอก package.json และ package-lock.json
COPY package*.json ./

# ติดตั้ง Dependencies
RUN npm install

# คัดลอกโค้ดทั้งหมด
COPY . .

# สร้างไฟล์ production โดยใช้คำสั่ง build
RUN npm run build

# Step 2: Serve the app with Nginx
FROM nginx:alpine

# คัดลอกไฟล์ static จากขั้นตอน build ไปยัง nginx เพื่อให้เสิร์ฟไฟล์ static
COPY --from=build /app/dist /usr/share/nginx/html

# เปิดพอร์ต 80 สำหรับ Nginx
EXPOSE 80

# เริ่ม Nginx
CMD ["nginx", "-g", "daemon off;"]
